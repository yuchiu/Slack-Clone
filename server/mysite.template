server {
    location /assets/files {
        alias /assets/files/;
        gzip_static on;
        expires max;
        add_header Cache-Control private;
    }
    location / {
        proxy_pass http://backend:3030;
    }
}